import ProfileClientPage from './client';

export const metadata = {
    title: 'Profile',
    description: 'Manage your profile and account settings on Diran.',
};

export default function ProfilePage() {
    return <ProfileClientPage />;
}
