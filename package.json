{
  "name": "diran-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/shared && bun run build && cd ../../apps/backend && bun run build && cd ../frontend && bun run build",
    "build": "cd packages/shared && bun run build && cd ../../apps/backend && bun run build && cd ../frontend && bun run build",
    "start": "cd apps/backend && bun run start"
  },
  "packageManager": "bun@1.3.0"
}
