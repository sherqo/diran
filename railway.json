{
  "build": {
    "buildCommand": "bun install && cd packages/shared && bun run build && cd ../../apps/backend && bunx prisma generate && bun run build"
  },
  "deploy": {
    "startCommand": "cd apps/backend && bun run start"
  }
}
