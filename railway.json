{
  "build": {
    "buildCommand": "bun install && cd packages/shared && bun run build && cd ../../apps/backend && bun run build && bunx prisma generate"
  },
  "deploy": {
    "startCommand": "cd apps/backend && bun run start"
  }
}
